fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv>=1.0.0
torch>=2.5.0
torchvision>=0.20.0
transformers>=4.57.0
huggingface-hub>=0.27.0
ultralytics>=8.0.0
pillow>=11.0.0
opencv-python>=4.10.0
numpy>=1.26.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-multipart>=0.0.20
httpx>=0.28.0
simple-lama-inpainting>=0.1.0
requests>=2.32.0

# Local LLM (llama.cpp)
# Recommended: compile with CUDA for GPU acceleration
#   CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python --force-reinstall --no-cache-dir
llama-cpp-python>=0.2.0

# Local Image Generation (Stable Diffusion XL / Z-Image)
# Diffusers loads Z-Image / SDXL automatically from HuggingFace Hub.
# Requires: ~6-8GB VRAM (fp16) or ~10GB (bfloat16).
# Z-Image support requires a recent diffusers (>=0.35.dev0). We install
# straight from source so the ZImagePipeline / ZImageImg2ImgPipeline
# classes are available without waiting for a PyPI release.
diffusers @ git+https://github.com/huggingface/diffusers.git
accelerate>=0.25.0
safetensors>=0.4.0

# Optional
pytest>=8.0.0

# GPU / CUDA
# For CUDA 12.x on Windows/Linux, replace torch/torchvision above with:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121